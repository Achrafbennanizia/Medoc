/**
 * TODO(deferred-features): Re-enable Datenschutz (DSGVO) UI — see
 * `docs/coordination/geplant.md` and `todos-deferred-features.md`.
 *
 * Backend IPC (`dsgvo_export_patient`, `dsgvo_erase_patient`) remains; only nav/route/links gated off.
 */

/** Set `true` when re-enabling the Datenschutz page (see deferred-features checklist). */
export const DATENSCHUTZ_UI_ENABLED = false;
