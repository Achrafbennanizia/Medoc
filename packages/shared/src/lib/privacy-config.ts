/**
 * TODO(deferred-features): Re-enable Privacy (DSGVO) UI — see
 * `docs/coordination/planned.md` and `todos-deferred-features.md`.
 *
 * Backend IPC (`dsgvo_export_patient`, `dsgvo_erase_patient`) remains; only nav/route/links gated off.
 */

/** Set `true` when re-enabling the Privacy page (see deferred-features checklist). */
export const PRIVACY_UI_ENABLED = false;
