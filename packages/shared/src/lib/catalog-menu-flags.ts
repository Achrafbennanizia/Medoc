/**
 * MVP blinds for catalog / template admin menus.
 * Documented in `docs/coordination/geplant.md`.
 */

/** Sidebar `/rezepte` + Verwaltung → Rezepte & Atteste vordefinieren (`/verwaltung/vorlagen`). */
export const REZEPTE_ATTESTE_MENU_ENABLED = false;

/** Sidebar `/leistungen` + Verwaltung hub „Leistungen“ row. */
export const LEISTUNGEN_MENU_ENABLED = false;

/** Verwaltung → Lager hub row + `/produkte` route (not in main sidebar). */
export const PRODUKTE_MENU_ENABLED = true;

/** Products table/detail — stock + min-stock column and form fields. */
export const PRODUKT_STOCK_UI_ENABLED = false;
