/**
 * MVP blinds for catalog / template admin menus.
 * Documented in `docs/coordination/planned.md`.
 */

/** Sidebar `/prescriptions` + Administration → Prescriptions & Certificates vordefinieren (`/administration/templates`). */
export const PRESCRIPTIONS_CERTIFICATES_MENU_ENABLED = false;

/** Sidebar `/services` + Administration hub „Services“ row. */
export const SERVICES_MENU_ENABLED = false;

/** Administration → Inventory hub row + `/products` route (not in main sidebar). */
export const PRODUCTS_MENU_ENABLED = true;

/** Products table/detail — stock + min-stock column and form fields. */
export const PRODUCT_STOCK_UI_ENABLED = false;
