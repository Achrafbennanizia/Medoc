/** FA-AUFG-03: polling interval on `/tickets` (Aufgaben + Tickets). */
export const POSTEINGANG_POLL_MS = 5_000;

/** Legacy flag: Posteingang nav/route removed — Aufgaben live on `/tickets`. */
export const POSTEINGANG_UI_ENABLED = false;
