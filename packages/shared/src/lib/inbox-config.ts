/** FA-AUFG-03: polling interval on `/tickets` (practice tasks + tickets). */
export const INBOX_POLL_MS = 5_000;

/** Legacy flag: Inbox nav/route removed — practice tasks live on `/tickets`. */
export const INBOX_UI_ENABLED = false;
