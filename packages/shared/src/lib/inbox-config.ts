/** FA-AUFG-03: polling interval on `/tickets` (Aufgaben + Tickets). */
export const INBOX_POLL_MS = 5_000;

/** Legacy flag: Inbox nav/route removed — Aufgaben live on `/tickets`. */
export const INBOX_UI_ENABLED = false;
