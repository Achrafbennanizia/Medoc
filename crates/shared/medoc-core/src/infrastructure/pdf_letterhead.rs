//! **Legacy flat path** — canonical module: [`crate::infrastructure::pdf::letterhead`].
//!
//! Pre-R2 wiring (commented out):
//!
//! ```ignore
//! // pub fn emit_letterhead(page: &mut PageBuilder, head: &Letterhead) { ... }
//! ```

pub use crate::infrastructure::pdf::letterhead::*;
