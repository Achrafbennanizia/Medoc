import { VerwaltungHubPage } from "../components/verwaltung-hub-page";

/** Inventory, products, order master data and contracts. */
export function VerwaltungLagerBestellwesenPage() {
    return <VerwaltungHubPage hubId="lager" />;
}
