import { VerwaltungHubPage } from "../components/verwaltung-hub-page";

/** Submenu "Team" under Verwaltung — section/description rows (same layout as Finanzen). */
export function VerwaltungTeamPage() {
    return <VerwaltungHubPage hubId="team" />;
}
