[package]
name = "medoc-codegen"
version = "0.1.0"
edition = "2021"
description = "Build-time codegen helpers for MeDoc (RBAC matrix + domain enums from YAML)."
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
# Wave B3 placeholder: actual codegen helpers (serde, serde_yaml, …) lift in Wave B4.
