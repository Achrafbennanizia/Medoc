# MeDoc codebase audit — 2026-05

**Work order:** WO-1 — audit only (no application source changes).  
**Operating rules:** Requested `docs/cursorrules-medoc.md` was **NOT FOUND**. Requested `docs/work-orders/` was **empty / NOT FOUND**. This audit follows **`/.cursor/rules/.cursorrules`** (“MeDoc Codebase Agent — Operating Rules”) and standard WO-1 intent: static review of `app/src/` and `app/src-tauri/`.

**Scope:** Frontend (`app/src/`), Rust backend (`app/src-tauri/`).  
**Method:** File reads + ripgrep; line-level evidence cited below.  
**Not run in this session:** `npm run build`, `npm test`, `cargo test`, live Tauri WebView IPC probes — IPC payload binding remains **inferred** from TS `snake_case` keys vs `#[tauri::command]` names unless noted.

---

## A. Schema / DTO drift (`schemas.ts`, `types.ts`, Rust DTOs / enums, SQLite `CHECK`)

| Area | Evidence | Current behavior | Expected / risk | Severity | Fix size |
|------|----------|------------------|-----------------|----------|----------|
| **TerminArt: Zod vs TS vs Rust vs DB** | `app/src/models/types.ts` 10–12 (`TERMIN_ART_VALUES` — five literals, no `NOTFALL` / `ROUTINE`); `app/src/lib/schemas.ts` 76–77, 79–87 (`TerminArtSchema` from tuple); `app/src-tauri/src/domain/enums.rs` 23–29; `app/src-tauri/src/infrastructure/database/connection.rs` 116 | Zod enums are driven from **`types.ts`**, matching Rust `TerminArt` and SQLite `CHECK`. | **No drift found** in this pass (older reports that listed `ROUTINE`/`NOTFALL` in Zod are **stale** vs current tree). | — | — |
| **TerminStatus: Zod vs TS vs DB vs serde** | `types.ts` 14–16 (`NICHT_ERSCHIENEN`); `schemas.ts` 77; `enums.rs` 34–42 (`#[serde(rename = "NICHT_ERSCHIENEN")]` + unit test); `connection.rs` 117 | Single wire format `NICHT_ERSCHIENEN` for serde/SQLite; Zod matches. | **Residual:** `termin_commands.rs` 22–23 treats **both** `NICHT_ERSCHIENEN` and legacy `NICHTERSCHIENEN` as terminal — implies historical rows could exist; migrations/cleanup policy should stay documented. | **Minor** (data hygiene / docs) | **S** |
| **ZahlungsArt: Zod vs Rust vs DB** | `types.ts` 24–25 (`RECHNUNG`, no `VERSICHERUNG`); `schemas.ts` 126–127; `enums.rs` 93–98; `connection.rs` 203 | Aligned. | — | — | — |
| **ZahlungsStatus: ordering** | `types.ts` 28–29; `schemas.ts` 127; `enums.rs` 103–108 | Same literals; TS tuple order differs from Rust variant order only cosmetically. | Unify for readability if desired. | **Minor** | **S** |
| **CreateLeistung Zod vs Rust `CreateLeistung`** | `schemas.ts` 179–184 (`name`, `beschreibung`, `kategorie`, `preis`); `app/src-tauri/src/domain/entities/leistung.rs` 16–22 | **Aligned**; wired via `leistung.controller.ts` (`parseOrThrow`). | — | — | — |
| **Geschlecht** | `schemas.ts` 45; `types.ts` 7–8; `enums.rs` 61–65; `connection.rs` 70 | Aligned. | — | — | — |
| **Patient read `geschlecht` vs enum on create** | `patient` entity patterns in Rust (not re-opened line-by-line here) | String storage + enum on create remains a normal pattern if normalized in repo. | Document only unless IO bugs appear. | **Minor** | **S** |

---

## B. Tauri IPC argument casing / binding

**Transport:** `app/src/services/tauri.service.ts` wraps `invoke` from `@tauri-apps/api/core`.

**Finding (sampled):** Controllers use **snake_case** argument objects consistent with typical `#[tauri::command]` parameter names (e.g. `patient_id`, `kasse_geprueft`, nested `data` wrappers) — **no systematic camelCase/tsify mismatch spotted** in spot checks.

**Caveat:** Tauri 2 runtime (de)serialization was **not** exercised with a running app; if a global rename policy were applied, calls could break — a **one-off packaged IPC smoke** remains useful.

| Severity | Fix size |
|----------|----------|
| — (none found in sampling) | — |

---

## C. RBAC alignment

### C.1 `allowed()` — TS vs Rust

**Evidence:** `app/src/lib/rbac.ts`; `app/src-tauri/src/application/rbac.rs`.

**Finding:** Not diffed line-by-line in this session; prior project expectation is parity. **Follow-up:** automated or scripted diff between action strings + role arms if regressions are a concern.

### C.2 Routes vs `ROUTE_VISIBILITY` / command palette

**Evidence:** `app/src/App.tsx` routes; `app/src/lib/command-palette-data.ts` 128–132.

| Issue | Detail | Severity | Fix size |
|-------|--------|----------|----------|
| **Hilfe palette `routePath`** | `hilfe` entry uses `routePath: "einstellungen"` while `href` is `/einstellungen?tab=hilfe`. RBAC for Hilfe in `rbac.ts` may still be correct, but **palette route key ≠ semantic `hilfe`**. | **Minor** | **S** |
| **Redirects without `RoleRoute` on source** | `App.tsx`: `verwaltung/tagesabschluss` / `verwaltung/finanzen-werkzeuge` → `Navigate`; targets guarded. Low risk. | **Minor** | **S** |

### C.3 Broad `personal.read` on Verwaltung subtree

**Evidence:** `rbac.ts` `ROUTE_VISIBILITY` (Verwaltung children).

Many admin/finance/inventory routes reuse **`personal.read`** where product intent might demand finer caps (compare with `verwaltung/bestellstamm` using `bestellung.read`).

| Severity | Fix size |
|----------|----------|
| **Major** (if product requires separation of duties) | **L** |

---

## D. Patient / sensitive data in `localStorage` / `sessionStorage` vs operating rules

**`.cursorrules` hard rule 2:** *“No patient-identifying data in localStorage.”* plus audit-relevant state should live in SQLite (`app_kv` / tables).

| Key / pattern | Location | Category | Notes |
|---------------|----------|----------|--------|
| `medoc-login-remember-email`, `medoc-login-remember-me` | `login.tsx` | **Identifier (staff)** | **Tension with hard rule 2** as written (email is personal data). Acceptable only if policy explicitly allows “remember email” UX; otherwise move to OS keychain or drop. |
| `medoc.akte.validation.v1.<patientId>` | `validation.controller.ts` / akte validation | **Patient-scoped** | Rule 2: should be **SQLite**-backed (migration path may exist — verify product truth). |
| `medoc.akte.plan.v2.*`, `medoc.akte.tipp.v1.*` | plan-next-termin | **Patient-scoped** | Same. |
| `medoc-termin-draft-*` | termin create drafts | **May embed `patient_id`** | Same. |
| `medoc-invoice-history-v1` | invoice history | **May embed patient refs** | Same. |
| `medoc-client-settings-v1`, invoice/praxis caches | various | **Practice / device** | Lower PII; still device-bound. |
| `medoc-ui-zoom` | `app-layout.tsx` | **UX** | `sessionStorage` — acceptable for UI. |

---

## E. Duplicate or near-duplicate page components

**Method:** Not re-run with sequence-matcher in this session (**NOT RUN** for full pairwise diff).

**Informal:** `leistungen.tsx` vs `produkte.tsx` historically ~high similarity — maintenance risk only.

| Severity | Fix size |
|----------|----------|
| **Minor** | **L** if extracting shared hub/list shell |

---

## F. Zod / controller wiring

**Controllers using `parseOrThrow` + schemas (non-exhaustive):** `patient`, `termin`, `zahlung`, `bestellung`, `personal`, `leistung`, `rezept`, `attest`, `feedback`, `bilanz-snapshot`, `akte` (behaviour/untersuchung/zahnbefund), per ripgrep.

**Finding:** Large share of IPC-creating controllers are schema-guarded; **residual** unused exports in `schemas.ts` possible — **not fully enumerated** this session.

| Severity | Fix size |
|----------|----------|
| **Minor** (if dead exports remain) | **S** |

---

## G. Accessibility — pattern sample (not exhaustive)

| Location | Concern | Severity | Fix size |
|----------|---------|----------|----------|
| Interactive `div` / non-button controls | Historical issue in dense pages (e.g. planning UIs) — **not re-verified file-by-file** | **Major** / **Minor** (case-by-case) | **M** |
| `dialog.tsx` | Focus trap / ESC — acceptable baseline | — | — |
| Icon-only controls | Must carry `aria-label` (`.cursorrules` DoD) | **Minor** if gaps | **S** |

---

## H. Error handling — pattern sample (not exhaustive)

**`.cursorrules` hard rule 3:** no silent swallow without toast, log, or commented rationale.

Sample risk areas to review in depth later: empty `catch`, `.catch(() => [])`, session `touchSession().catch(() => {})`, localStorage try/catch — **not exhaustively listed** in this pass (~100+ `catch` sites).

| Severity | Fix size |
|----------|----------|
| **Major** / **Minor** (per site) | **S–M** |

---

## Summary table (severity counts — approximate)

| Section | Blocker | Major | Minor |
|---------|---------|-------|-------|
| A | 0 | 0 | 1 |
| B | 0 | 0 | 0 |
| C | 0 | 1 | 2 |
| D | — (policy) | — | — |
| E | 0 | 0 | 1 |
| F | 0 | 0 | 1 |
| G | 0 | 0+ | sample |
| H | 0 | sample | sample |

---

## Validation

| Check | Result |
|-------|--------|
| `npm run build` / `npm test` / `cargo test` | **NOT RUN** (WO-1 audit-only; no execution this session) |
| Live IPC serde probe | **NOT RUN** |

---

## Operating rules cross-check (from `.cursor/rules/.cursorrules`)

| Rule theme | Audit hook |
|------------|------------|
| BE source of truth | §A alignment; FE should not widen Zod beyond BE **(currently OK for sampled enums)** |
| No patient ID in localStorage | §D — **several patient-scoped keys remain**; contradicts strict reading of rule 2 |
| No silent errors | §H — sample only; full pass still needed |
| RBAC | §C — palette minor drift; possible over-broad `personal.read` |
| Dependencies | Not assessed |

---

*End of audit (WO-1). **No application source files were modified** to produce this document.*
